{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-strain",
            "type": "shell",
            "command": "cargo build --bin argus -F strain"
        },
        {
            "label": "build-pressure",
            "type": "shell",
            "command": "cargo build --bin argus -F pressure"
        },
        {
            "label": "build-temperature",
            "type": "shell",
            "command": "cargo build --bin argus -F temperature"
        }
    ]
}